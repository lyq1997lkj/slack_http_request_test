{
  "name": "slack-debug-tool",
  "version": "1.0.0",
  "description": "Simple tool to debug Slack webhook requests",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}